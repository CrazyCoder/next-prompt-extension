{
	"name": "@jetserge/next-prompt",
	"version": "0.3.1",
	"description": "pi and Oh My Pi (OMP) coding-agent extension that suggests the next logical prompt after an agent turn settles (inline ghost, below-editor widget, or both).",
	"keywords": [
		"pi-package",
		"omp"
	],
	"license": "MIT",
	"type": "module",
	"main": "next-prompt.ts",
	"files": [
		"next-prompt.ts",
		"README.md",
		"LICENSE"
	],
	"repository": {
		"type": "git",
		"url": "git+https://github.com/CrazyCoder/next-prompt-extension.git"
	},
	"homepage": "https://github.com/CrazyCoder/next-prompt-extension",
	"bugs": {
		"url": "https://github.com/CrazyCoder/next-prompt-extension/issues"
	},
	"engines": {
		"node": ">=18"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"typecheck:omp": "tsc --noEmit -p tsconfig.omp.json",
		"verify:package": "npm pack --dry-run --json | node -e 'let s=\"\";process.stdin.on(\"data\",d=>s+=d).on(\"end\",()=>{const d=JSON.parse(s);const pkg=Object.values(d)[0];const got=pkg.files.map(f=>f.path).sort();const want=[\"LICENSE\",\"README.md\",\"next-prompt.ts\",\"package.json\"].sort();if(JSON.stringify(got)!==JSON.stringify(want)){console.error(\"package contents mismatch: got \"+JSON.stringify(got)+\" want \"+JSON.stringify(want));process.exit(1)}console.log(\"package contents exact: \"+got.join(\", \"))})'",
		"verify:tarball": "node scripts/verify-tarball.mjs"
	},
	"publishConfig": {
		"access": "public"
	},
	"pi": {
		"extensions": [
			"./next-prompt.ts"
		]
	},
	"peerDependencies": {
		"@earendil-works/pi-ai": "*",
		"@earendil-works/pi-coding-agent": "*",
		"@earendil-works/pi-tui": "*"
	},
	"peerDependenciesMeta": {
		"@earendil-works/pi-ai": {
			"optional": true
		},
		"@earendil-works/pi-coding-agent": {
			"optional": true
		},
		"@earendil-works/pi-tui": {
			"optional": true
		}
	},
	"devDependencies": {
		"@earendil-works/pi-ai": "0.85.1",
		"@earendil-works/pi-coding-agent": "0.85.1",
		"@earendil-works/pi-tui": "0.85.1",
		"@oh-my-pi/pi-ai": "17.2.13",
		"@oh-my-pi/pi-coding-agent": "17.2.13",
		"@oh-my-pi/pi-tui": "17.2.13",
		"@types/bun": "1.3.14",
		"typescript": "^7.0.2"
	}
}
